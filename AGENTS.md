# Flash-Hog
This repo implements FlashAttention with support for forward, backward (vjp) and second-order backward (vjp of vjp).

## Conventions
The environment is managed with `uv`. Only run Python using `uv`.

Keep your edits minimal when possible.